<%= form_for(fight_style) do |f| %>
  <% if fight_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fight_style.errors.count, "error") %> prohibited this fight_style from being saved:</h2>

      <ul>
      <% fight_style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
